#[extern "C"]
def __mvm_array_malloc[T](element_count usize) T[*]

def main() i32 {
    var values = __mvm_array_malloc[u8](4_usize)
    var raw = &values(0)
    if raw == null {
        ret 1
    }
    ret 0
}

ret main()
